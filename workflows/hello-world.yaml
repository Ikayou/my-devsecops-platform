apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: hello-argo
  namespace: argo 
spec:
  entrypoint: main
  templates:
    - name: main
      container:
        image: alpine
        command: [echo]
        args: ["Endlich Argoworkflow!!"]